component: "etcd"

image:
  source: "quay.io/coreos/etcd"
  tag: "v3.2.9"
  pullPolicy: "Always"

service:
  name: etcdvault
  peerPort: 3380
  clientPort: 3379
  replicas: 3
  cpu: "100m"
  memory: "512Mi"
  dataDir: "/ephemeral"
  terminationGracePeriodSeconds: 30

storage:
  storageClass: "default"
  size: 1Gi
  mount: "ephemeral"
  accessModes:
    - ReadWriteOnce
